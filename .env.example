# demo: browser-only preview. api: connect to a Spring Boot backend.
VITE_DATA_MODE=api
# Browser calls the same-origin server route. No credentials in VITE_ values.
VITE_API_BASE_URL=/api
# Server-only upstream. Configure HTTPS here once available.
FOLLOWUP_BACKEND_URL=http://13.124.207.246:8080
